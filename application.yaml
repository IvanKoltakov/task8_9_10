server:
  port : 8081
spring:
  datasource:
    url: jdbc:postgresql://localhost:8080/first_db
    username: postgres
    password: pass
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update